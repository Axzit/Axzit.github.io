# Axzit

This repository is now a modern React web app scaffold for Axzit.

## Getting started
1. Install dependencies with `npm install`.
2. Start the development server with `npm run dev`.
3. Build for production with `npm run build`.

## About
Axzit is a futuristic portfolio and brand presence built with React and Vite.
The app uses accessible semantic markup and responsive styling.
